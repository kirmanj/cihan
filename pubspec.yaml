name: cihan
description: Cihan Translation.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.2

  gradient_borders: ^0.2.0
  intl: ^0.17.0
  flutter_map:
  http:
  shared_preferences:
  carousel_slider: ^4.1.1
  easy_dynamic_theme: ^2.0.0
  url_launcher: 
  countup: ^0.1.4
  provider:
  path_provider:
  whatsapp_unilink: 
  file_picker:
  flutter_email_sender: 
  dio:
  path:
  mailer:


  


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  fonts:
    - family: Montserrat
      fonts:
        - asset: fonts/Montserrat-Regular.ttf

    - family: Electrolize=
      fonts:
        - asset: fonts/Electrolize-Regular.ttf

  assets:
    - assets/images/
